[pytest]
markers =
    integration: marks tests as integration tests that require live services
                 (QDRANT_URL, QDRANT_API_KEY env vars + ingestion service).
                 Skipped automatically when credentials are absent.
                 Run explicitly with: pytest -m integration

# Enforce that all markers are declared — catches typos in @pytest.mark usage.
addopts = --strict-markers
