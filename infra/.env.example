# Enterprise SecureChat — Environment Variables
# Copy this file to .env and fill in real values before running docker compose.
# Never commit .env to git.

# ── Neon PostgreSQL ───────────────────────────────────────────────────────────
# JDBC format — required by Spring Boot / HikariCP (note the jdbc: prefix)
SPRING_DATASOURCE_URL=jdbc:postgresql://<endpoint>.neon.tech/fga_registry?sslmode=require
SPRING_DATASOURCE_USERNAME=<neon-username>
SPRING_DATASOURCE_PASSWORD=<neon-password>

# ── Auth0 ─────────────────────────────────────────────────────────────────────
# Tenant issuer URL (trailing slash required) and API audience identifier
AUTH0_ISSUER_URI=https://<tenant>.us.auth0.com/
AUTH0_AUDIENCE=api.enpsecurechat.com

# ── Qdrant ────────────────────────────────────────────────────────────────────
# API key — generate a strong random string (e.g. openssl rand -hex 32)
QDRANT_API_KEY=<strong-random-api-key>
# Qdrant URL: Qdrant Cloud URL or http://qdrant:6333 for local Docker
QDRANT_URL=https://<cluster-id>.<region>.aws.cloud.qdrant.io

# ── Anthropic / Claude ────────────────────────────────────────────────────────
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── Internal telemetry endpoint (ADR-002) ────────────────────────────────────
# Shared secret for GET /internal/llm-metrics — generate with e.g. openssl rand -hex 32.
# Must match the value monitoring-links sends in its X-Internal-Key header.
INTERNAL_METRICS_KEY=<strong-random-shared-secret>
