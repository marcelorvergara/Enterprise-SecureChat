name: Deploy Ingestion

on:
  push:
    branches: [main]
    paths:
      - 'ingestion/**'
      - '.github/workflows/ingestion.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: gcloud auth configure-docker us-east4-docker.pkg.dev --quiet

      - name: Build and push image
        run: |
          docker build -t us-east4-docker.pkg.dev/enp-securechat/securechat-repo/securechat-ingestion:latest .
          docker push us-east4-docker.pkg.dev/enp-securechat/securechat-repo/securechat-ingestion:latest
        working-directory: ingestion

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy securechat-ingestion \
            --image us-east4-docker.pkg.dev/enp-securechat/securechat-repo/securechat-ingestion:latest \
            --region=us-east4
